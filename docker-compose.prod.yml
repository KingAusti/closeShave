version: '3.8'

# Production docker-compose configuration
# Usage: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  backend:
    environment:
      - PYTHONUNBUFFERED=1
      # Add production environment variables here
      # - DATABASE_URL=...
      # - API_KEY=...
    # Remove volume mounts in production if using cloud storage
    # volumes:
    #   - ./backend/data:/app/data
    #   - ./backend/logs:/app/logs
    #   - ./backend/config:/app/config

  frontend:
    # Production nginx configuration can be customized
    # volumes:
    #   - ./frontend/nginx.prod.conf:/etc/nginx/conf.d/default.conf

