# CloseShave Environment Configuration

# Port Configuration
# Override these to use different ports if the defaults are already in use

# Backend API port (default: 8000)
BACKEND_PORT=8000

# Frontend web server port (default: 80)
# Note: On macOS/Linux, ports below 1024 require sudo/root privileges
# Consider using 8080 or another port above 1024 for local development
FRONTEND_PORT=80

# Example: Use alternative ports to avoid conflicts
# BACKEND_PORT=8001
# FRONTEND_PORT=8080
